# Basic configuration
port 6379
bind 0.0.0.0
protected-mode yes
requirepass lumen_redis_password

# Persistence
save 900 1
save 300 10
save 60 10000
appendonly yes
appendfilename "appendonly.aof"

# Memory management
maxmemory 256mb
maxmemory-policy allkeys-lru